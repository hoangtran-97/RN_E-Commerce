import React from "react";
import { SafeAreaView } from "react-native";
import { Search } from "../../Components/Search";

export const Home = () => {
    return (
        <SafeAreaView>
            <Search />
        </SafeAreaView>
    );
};
