import React from "react";
import { Text, SafeAreaView } from "react-native";

export const Setting = () => {
    return (
        <SafeAreaView>
            <Text>Setting</Text>
        </SafeAreaView>
    );
};
