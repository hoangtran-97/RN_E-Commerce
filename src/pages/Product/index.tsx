import React from "react";
import { View, Text, SafeAreaView } from "react-native";

export const Product = () => {
    return (
        <SafeAreaView>
            <Text>Product</Text>
        </SafeAreaView>
    );
};
